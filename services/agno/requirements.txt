fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0
asyncpg>=0.29.0
pgvector>=0.3.0
psycopg2-binary>=2.9.9
markitdown[all]>=0.1.0

# Note: pgvector package is needed for vector type operations with asyncpg.
# Note: markitdown[all] is needed for file conversion (docx, pdf, xlsx, pptx, images, audio, etc.)
# The agno Docker container installs packages via docker-compose command:
#   pip install fastapi uvicorn asyncpg httpx pgvector markitdown[all]
